class Heaps extends BaseFlatSpec {
  // Return a boolean to predicate => Is there a duplicate?
  "703 - Kth Largest" should "Find the Kth Largest lol" in {
    val res = false
    assertResult(true)(res)
  }
}
